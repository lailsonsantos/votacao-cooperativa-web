# URL base da API, SEM o sufixo /api/v1.
#
# Externalizada pelo mesmo motivo que o backend externaliza as URLs de callback:
# o mesmo build precisa servir desenvolvimento, rede local (para abrir de um
# celular) e nuvem. Copie para .env.local e ajuste.
VITE_API_BASE_URL=http://localhost:8080
